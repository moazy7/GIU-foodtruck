<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>GIU FoodTruck | Login</title>

    <link rel="stylesheet" href="/styles/bootstrap.min.css" />
    <link rel="stylesheet" href="/styles/style.css" />

    <script src="/js/jquery-2.2.0.min.js"></script>
    <script src="/js/bootstrap.min.js"></script>
    <script src="/src/login.js"></script>
  </head>

  <body class="auth-page">
    <!-- Top Navbar -->
    <nav class="navbar navbar-inverse auth-navbar">
      <div class="container">
        <div class="navbar-header">
          <a class="navbar-brand auth-brand" href="/">
            <span class="auth-dot">🍟</span> GIU FoodTruck
          </a>
        </div>

        <ul class="nav navbar-nav navbar-right auth-navbtns">
          <li class="active"><a class="auth-pill active" href="/">Login</a></li>
          <li><a class="auth-pill" href="/register">Register</a></li>
        </ul>
      </div>
    </nav>

    <!-- Center Card -->
    <div class="auth-center">
      <div class="auth-card">
        <div class="auth-icon">🍔</div>
        <h2 class="auth-title">Welcome Back!</h2>
        <p class="auth-subtitle">Sign in to continue</p>

        <div id="msg" class="alert" style="display:none;"></div>

        <form id="loginForm" class="auth-form">
          <div class="form-group">
            <label class="auth-label" for="email">Email Address</label>
            <input
              id="email"
              name="email"
              type="text"
              class="form-control auth-input"
              placeholder="Enter your email..."
            />
          </div>

          <div class="form-group">
            <label class="auth-label" for="password">Password</label>
            <input
              id="password"
              name="password"
              type="password"
              class="form-control auth-input"
              placeholder="Enter your password..."
            />
          </div>

          <button id="submit" type="submit" class="btn auth-btn btn-block">
            SIGN IN
          </button>

          <p class="auth-bottom text-center">
            Don't have an account?
            <a class="auth-link" href="/register">Register here</a>
          </p>
        </form>
      </div>
    </div>
  </body>
</html>
